export type ThemeType = {
  mode: 'light' | 'dark';
  background: string;
  text: string;
  card: string;

  primary: string;
  border: string;
  danger: string;
  success: string;
  blue1: string;
  blue2: string;
  orange: string;
  gray: string;
  shadow: string;
  white: string;
};
