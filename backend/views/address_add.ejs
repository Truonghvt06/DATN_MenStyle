<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Thêm địa chỉ</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="container my-4">
  <h2>Thêm địa chỉ mới</h2>

  <form method="POST" action="/address/add">
    <input type="hidden" name="returnTo" value="<%= returnTo %>" />
    <input type="hidden" name="userId" value="<%= userId %>" />
  
    <div class="mb-3">
      <label>Họ tên người nhận</label>
      <input class="form-control" name="recipient_name" value="<%= user.name %>" required />
    </div>
  
    <div class="mb-3">
      <label>Số điện thoại</label>
      <input class="form-control" name="phone" value="<%= user.phone %>" required />
    </div>
  
    <div class="mb-3">
      <label>Địa chỉ chi tiết</label>
      <input class="form-control" name="address_line" required />
    </div>
  
    <div class="mb-3">
      <label>Tỉnh/Thành</label>
      <input class="form-control" name="province" required />
    </div>
  
    <div class="mb-3">
      <label>Quận/Huyện</label>
      <input class="form-control" name="district" required />
    </div>
  
    <div class="mb-3">
      <label>Phường/Xã</label>
      <input class="form-control" name="ward" required />
    </div>
  
    <button class="btn btn-primary">Thêm địa chỉ</button>
    <a href="/accounts/<%= userId %>/cart/checkout" class="btn btn-secondary">Hủy</a>
  </form>
  
</body>
</html>
