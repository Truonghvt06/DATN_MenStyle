<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Thống kê - MenStyle</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', sans-serif;
    }
    .chart-card {
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.05);
      background: #fff;
      margin-bottom: 30px;
    }
    .chart-title {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 15px;
    }
  </style>
</head>
<body class="p-4 bg-light">
  <div class="container-fluid">
    <h2 class="mb-4">📊 Thống kê hệ thống</h2>

    <!-- Dữ liệu từ server truyền vào qua thẻ ẩn -->
    <div id="ejs-data"
         data-user-labels='<%- JSON.stringify(userLabels) %>'
         data-new-users='<%- JSON.stringify(newUsersData) %>'
         data-daily-labels='<%- JSON.stringify(dailyLabels) %>'
         data-revenue-daily='<%- JSON.stringify(revenueDaily) %>'
         data-monthly-labels='<%- JSON.stringify(monthlyLabels) %>'
         data-revenue-monthly='<%- JSON.stringify(revenueMonthly) %>'>
    </div>

    <div class="row g-4">
      <div class="col-lg-4">
        <div class="chart-card" style="background-color: #f06292; color: white;">
          <div class="chart-title">Người dùng mới</div>
          <canvas id="newUsersChart"></canvas>
        </div>
      </div>

      <div class="col-lg-4">
        <div class="chart-card" style="background-color: #66bb6a; color: white;">
          <div class="chart-title">Doanh thu theo ngày</div>
          <canvas id="dailyRevenueChart"></canvas>
        </div>
      </div>

      <div class="col-lg-4">
        <div class="chart-card" style="background-color: #212121; color: white;">
          <div class="chart-title">Doanh thu theo tháng</div>
          <canvas id="monthlyRevenueChart"></canvas>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Lấy dữ liệu từ thẻ div#ejs-data
    const dataDiv = document.getElementById('ejs-data');
    const userLabels = JSON.parse(dataDiv.dataset.userLabels);
    const newUsersData = JSON.parse(dataDiv.dataset.newUsers);
    const dailyLabels = JSON.parse(dataDiv.dataset.dailyLabels);
    const revenueDaily = JSON.parse(dataDiv.dataset.revenueDaily);
    const monthlyLabels = JSON.parse(dataDiv.dataset.monthlyLabels);
    const revenueMonthly = JSON.parse(dataDiv.dataset.revenueMonthly);

    // Người dùng mới
    new Chart(document.getElementById('newUsersChart'), {
      type: 'line',
      data: {
        labels: userLabels,
        datasets: [{
          label: 'Người dùng mới',
          data: newUsersData,
          borderColor: '#fff',
          backgroundColor: '#fff',
          fill: false,
          tension: 0.3
        }]
      },
      options: {
        plugins: { legend: { labels: { color: '#fff' } } },
        scales: {
          x: { ticks: { color: '#fff' } },
          y: { ticks: { color: '#fff' } }
        }
      }
    });

    // Doanh thu theo ngày
    new Chart(document.getElementById('dailyRevenueChart'), {
      type: 'line',
      data: {
        labels: dailyLabels,
        datasets: [{
          label: 'Doanh thu theo ngày',
          data: revenueDaily,
          borderColor: '#fff',
          backgroundColor: '#fff',
          fill: false,
          tension: 0.3
        }]
      },
      options: {
        plugins: { legend: { labels: { color: '#fff' } } },
        scales: {
          x: { ticks: { color: '#fff' } },
          y: { ticks: { color: '#fff' } }
        }
      }
    });

    // Doanh thu theo tháng
    new Chart(document.getElementById('monthlyRevenueChart'), {
      type: 'line',
      data: {
        labels: monthlyLabels,
        datasets: [{
          label: 'Doanh thu theo tháng',
          data: revenueMonthly,
          borderColor: '#fff',
          backgroundColor: '#fff',
          fill: false,
          tension: 0.3
        }]
      },
      options: {
        plugins: { legend: { labels: { color: '#fff' } } },
        scales: {
          x: { ticks: { color: '#fff' } },
          y: { ticks: { color: '#fff' } }
        }
      }
    });
  </script>
</body>
</html>
